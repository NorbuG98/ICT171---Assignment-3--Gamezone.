# HTTPS Setup

HTTPS was configured using Certbot and Let's Encrypt.

Commands used:

sudo apt update
sudo apt install certbot python3-certbot-nginx

sudo certbot --nginx

The SSL certificate was successfully installed and configured for the GameZone website.
